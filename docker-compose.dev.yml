# Dev overlay — expose qdrant + api for local `pnpm dev` / `pnpm dev:web`.
# Does not publish web (Vite uses :5173). Optional nginx preview: see docker-compose.prod.yml port 8080.

services:
  qdrant:
    ports:
      - "6333:6333"

  api:
    ports:
      - "3000:3000"
