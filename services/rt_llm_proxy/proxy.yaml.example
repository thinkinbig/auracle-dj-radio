# Copy to proxy.yaml (auto-loaded from the working directory; override path with
# -config). Provider behavior only; infrastructure stays on CLI flags / env.
# Credentials (GEMINI_API_KEY) are NEVER read from here.

gemini:
  system_prompt: "You are a helpful, concise voice assistant."
  # tools:
  #   - name: get_weather
  #     description: Get the current weather for a city
  #     parameters:
  #       type: object
  #       properties:
  #         city:
  #           type: string
  #       required: [city]
